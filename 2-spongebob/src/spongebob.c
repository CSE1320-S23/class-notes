#include <stdio.h>

// TODO: Write a script that takes the first argument supplied form the command line and converts it into spongebob text.

int main(int argc, char *argv[])
{
    printf("%s", argv[1]);
}
